//  usando 'e' para ser v ' verdadeiro, todos os elementos tem q ser verdadeiros, ao 
// contrario é f 'falso' 

// v e v -> v   // são operadores binario
// v e f -> f
// f e ? -> f

// No caso de uma operação com 'ou' , basta um dar 'v' verdadeiro, para o resultado ser 'v'

// v ou ? -> v // operadores binario
// f ou v -> v
// f ou f -> f
  

// No caso do "ou exclusivo", tem q ser sempre diferente,para o resultado dar verdadeiro 'v', se 
// forwem iguais da 'f' falso.

// v xor v -> f  // operadores binario
// v xor f -> v
// f xor v -> v
// f xor f -> 

// A negação logica em javascript q é a '!', sendo a negação de verdadeiro 'v' vira 'f' falso e
// a negação de 'f' falso , vira 'v' verdadeiro.

// !v -> f // operadores unarios ( um operador)
// !f -> v