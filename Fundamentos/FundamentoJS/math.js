const raio = 5.6
const area = Math.PI*Math.pow(raio, 2) // "PI" é uma constante e ".pow" calcula a potência

console.log(area.toFixed(2))  //Usei o "tiFixed(2)" para definir 2 casas
console.log(typeof Math)  // Math é denominado um objeto